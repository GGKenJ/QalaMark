
Ниже — **идеальное распределение задач по 3 участникам** в команде QalaMark.
Формат: кто, что делает, какие файлы/модули создаёт, какие зависимости нужны, и главное — *что не усложнять*.

---

# 🧠 Общая структура проекта

```
QalaMark/
 ├── backend/        # Node.js + Express + PostgreSQL
 ├── frontend/       # React (Vite или CRA)
 └── db/             # SQL init scripts
```

---

# 👨‍💻 1. БЭКЕНДЕР

*(главный — отвечает за архитектуру, API, БД, связку фронта и сервера)*

### 📍 Цель:

Обеспечить полностью работающий REST API, который может использоваться без задержек фронтом.
Фокус — стабильность, понятная структура, отсутствие лишних зависимостей.

---

### 🔧 Задачи по этапам

#### 1. Структура проекта:

Создать:

```
backend/
 ├── src/
 │    ├── index.js
 │    ├── db.js
 │    ├── routes/
 │    │    ├── feedback.js
 │    │    ├── auth.js
 │    │    └── admin.js
 │    ├── middleware/
 │    │    └── upload.js
 │    ├── utils/
 │    │    └── categorize.js
 └── .env
```

#### 2. Настройка PostgreSQL

Создать таблицы:

* `users(id, username, password_hash, role)`
* `feedbacks(id, title, description, category, lat, lon, photo_url, votes, status, created_at)`
* `categories(name, keywords[])`

⚙️ **Инструменты:** `pg`, `bcrypt`, `dotenv`, `multer`.

#### 3. Реализовать основные API эндпоинты:

| Эндпоинт                   | Метод | Описание                                           |
| -------------------------- | ----- | -------------------------------------------------- |
| `/api/register`            | POST  | регистрация (username + password)                  |
| `/api/login`               | POST  | авторизация, возвращает JWT                        |
| `/api/feedback`            | POST  | создать жалобу (с фото и координатой)              |
| `/api/feedbacks`           | GET   | получить список (фильтры: категория, статус, bbox) |
| `/api/feedback/:id/vote`   | POST  | добавить лайк                                      |
| `/api/feedback/:id/status` | PATCH | изменить статус (только admin)                     |

#### 4. Написать функцию `categorize(text)`

* Простая keyword-система:

  * “дорога”, “яма”, “асфальт” → road
  * “мусор”, “запах” → ecology
  * “фонарь”, “жарық” → lighting
  * Иначе “other”

#### 5. Настроить CORS и JSON body parser

#### 6. Документация API (Postman export или README.md)

---

### ⚠️ Не усложнять:

* JWT без refresh-токенов.
* Фото хранить локально (папка `/uploads`).
* Не ставить TypeScript, не писать сложные ORM (только `pg`).
* Не делать роли с гранулярными правами (только `user` и `admin`).

---

### 🧩 Задачи поддержки фронтов:

* Настроить CORS, чтобы React без ошибок доставал `/api/...`.
* Подключить фронтам фиктивные данные (если API не готов).
* Быстро править эндпоинты под их fetch-запросы.
* Обеспечить миграцию и сидинг тестовыми данными.

---

# 👩‍💻 2. ФРОНТЕНД 1 — **Авторизация, регистрация, навигация**

*(ответственный за “скелет” фронта — вход, выход, навигация, UI-базис)*

---

### 🎯 Цель:

Создать каркас сайта, где всё подключено и можно безопасно работать:
регистрация, логин, переходы между страницами, навбар, роутинг.

---

### 🧱 Структура проекта

```
frontend/
 ├── src/
 │    ├── main.jsx
 │    ├── App.jsx
 │    ├── api/ (все fetch функции)
 │    ├── pages/
 │    │    ├── Login.jsx
 │    │    ├── Register.jsx
 │    │    ├── Home.jsx
 │    │    ├── MapPage.jsx
 │    │    ├── AdminPanel.jsx
 │    ├── components/
 │    │    ├── Navbar.jsx
 │    │    ├── ProtectedRoute.jsx
 │    └── utils/
 │         └── auth.js
```

---

### 🔧 Задачи

1. Установить React Router.
2. Сделать страницы:

   * `/register` — форма (username, password)
   * `/login` — форма (fetch → /api/login → сохранить токен в localStorage)
   * `/` — редирект на карту (если залогинен)
3. Реализовать `ProtectedRoute` компонент:

   * Проверяет токен в `localStorage`.
   * Если нет — редирект на `/login`.
4. Реализовать `Navbar`:

   * Кнопки: “Карта”, “Добавить”, “Админ” (если роль admin).
   * Справа — имя пользователя, кнопка “Выйти”.
5. Создать модуль `api/auth.js`:

   * `login(username, password)`
   * `register(username, password)`
   * `getUser()` — расшифровывает токен.

---

### ⚠️ Не усложнять:

* Не внедрять Redux или Context — хватит useState + localStorage.
* Не делать красивый UI — форма и навбар в минимальном виде.
* Не валидировать сложные пароли.

---

### ✅ Когда фронт 1 закончит:

* Авторизация работает.
* При входе → можно открыть карту.
* При выходе → localStorage очищается.
* Остальные страницы доступны фронту 2.

---

# 👨‍🎨 3. ФРОНТЕНД 2 — **Основная функциональность**

*(карта, жалобы, лайки, добавление, админ-панель)*

---

### 🎯 Цель:

Реализовать функционал самого QalaMark — пользователь видит проблемы, может добавить новые, лайкать, фильтровать.

---

### 🧱 Структура компонентов:

```
frontend/src/pages/
 ├── MapPage.jsx
 ├── AddFeedback.jsx
 ├── AdminPanel.jsx

frontend/src/components/
 ├── FeedbackMarker.jsx
 ├── FeedbackForm.jsx
 ├── FeedbackList.jsx
```

---

### 🔧 Задачи по частям

#### 1. Карта (MapPage.jsx)

* Использовать **Leaflet**.
* При загрузке: `GET /api/feedbacks` → показать маркеры.
* При клике на маркер → popup с title, category, votes, кнопкой “Подтвердить”.
* Кнопка “Подтвердить” → `POST /api/feedback/:id/vote` → обновить счётчик.
* Фильтры: select категории (ecology/road/lighting/other).

#### 2. Добавление жалобы (AddFeedback.jsx)

* Форма: title, description, фото (input type=file).
* Кнопка “Определить местоположение” (использовать `navigator.geolocation`).
* После submit → `POST /api/feedback` → success → редирект на карту.
* Показывать preview фото.

#### 3. Админ-панель (AdminPanel.jsx)

* Таблица со всеми жалобами.
* Колонки: title, category, votes, status, кнопка “Изменить статус”.
* Возможность фильтра по статусу (`new`, `in_progress`, `done`).
* `PATCH /api/feedback/:id/status`.

---

### ⚠️ Не усложнять:

* Не использовать сложные UI-библиотеки (Material UI, Bootstrap — максимум).
* Не внедрять real-time обновления.
* Фото — просто загружать и отображать URL (без миниатюр и ресайза).
* Не делать карту слишком сложной — стандартный zoom и маркеры достаточно.

---

### ✅ Когда фронт 2 закончит:

* Рабочая карта с жалобами и лайками.
* Возможность добавить жалобу.
* Админ-панель работает (меняет статусы).

---

# 🧩 Финальная интеграция

После полуночи — **совместная проверка**:

1. Бэкендер запускает сервер.
2. Фронты настраивают `.env` → `VITE_API_URL=http://localhost:3000`
3. Тестовый прогон:

   * Регистрация
   * Логин
   * Добавление жалобы
   * Отображение на карте
   * Лайк
   * Админ статус

---

# ⏰ Тайминг “вечер → утро”

| Время       | Участник | Задачи                                 |
| ----------- | -------- | -------------------------------------- |
| 18:00–20:00 | Бэкендер | структура API, схема БД                |
| 18:00–21:00 | Фронт1   | регистрация + логин                    |
| 20:00–00:00 | Фронт2   | карта + отображение жалоб              |
| 00:00–02:00 | Бэкендер | подключение фронтов, тесты эндпоинтов  |
| 02:00–04:00 | Фронт1   | навигация и редиректы                  |
| 02:00–05:00 | Фронт2   | форма добавления + лайки               |
| 05:00–07:00 | Все      | админ-панель + отладка + демо-сценарий |

---

Хочешь, я дополнительно сделаю **архитектурную диаграмму** (стрелками — кто с кем взаимодействует и какие API вызываются)? Она поможет при устной защите проекта.



Отвечаю как **UI/UX-дизайнер с опытом в civic tech-проектах и экоплатформах (лауреат Almaty Smart City Hackathon)**.

---

### 🎨 **Цветовая схема для QalaMark**

Ты уже выбрал белый фон и зелёные элементы — это отличная база, потому что:

* Белый = чистота, прозрачность, открытость (идеально для гражданских инициатив)
* Зелёный = развитие, экология, доверие

Ниже — оптимальная комбинация, которая выглядит современно и не перегружает интерфейс.

---

### ✅ **Основная палитра**

| Назначение                            | Цвет | Код       | Комментарий                                                           |
| ------------------------------------- | ---- | --------- | --------------------------------------------------------------------- |
| **Основной зелёный (Primary)**        | 🌿   | `#2E7D32` | Глубокий природный зелёный — вызывает доверие, не кислотный.          |
| **Акцентный зелёный (Hover, кнопки)** | 🍃   | `#43A047` | Более яркий, используется при наведении или для CTA-кнопок.           |
| **Фон страницы**                      | ⚪    | `#FFFFFF` | Белый, максимум контраста и чистоты.                                  |
| **Вторичный фон (карточки, блоки)**   | 🩶   | `#F9FAFB` | Мягкий серовато-белый, отделяет секции.                               |
| **Текст основной**                    | ⚫    | `#212121` | Глубокий графитовый, не чисто чёрный — лучше для глаз.                |
| **Текст второстепенный / подсказки**  | ⚫    | `#616161` | Для описаний и мелких деталей.                                        |
| **Ошибки / предупреждения**           | 🔴   | `#D32F2F` | Например, при неверном вводе или проблеме.                            |
| **Информационный / нейтральный**      | 🔵   | `#0288D1` | Можно использовать для инфо-сообщений (например, «данные обновлены»). |

---

### 🌗 **Контраст и атмосфера**

* Делай **максимально светлый интерфейс** — белые блоки, тени `rgba(0,0,0,0.05)` для глубины.
* Акценты — только на **действиях**: кнопки, маркеры, ховер, ссылки.
* Зеленый должен ассоциироваться с "действием" (Добавить проблему, Голосовать, и т.д.)
* Для карты — подложка **серо-зеленоватая**, чтобы маркеры ярко выделялись (например, Google Maps light mode).

---

### 🧭 **Примеры применения**

* **Логотип / navbar:** белый фон + тёмно-зелёный текст `QalaMark`.
* **Кнопки:**

  * Primary: `#2E7D32` (текст белый)
  * Hover: `#43A047`
* **Карточки проблем:** белый фон, лёгкая тень, лайки/иконки — зелёные.
* **Маркер на карте:**

  * Проблема дороги — оранжевый
  * Экология / запах — зелёный
  * Другие категории — серый

---

### ⚡ **Рекомендации**

* Избегай кислотных оттенков (`#00FF00`, `#66FF66`) — они дешево смотрятся.
* Не добавляй градиенты — плоский (flat) дизайн быстрее реализуется.
* Шрифт — **Inter** или **Roboto**. Размеры: 16–18px базовый, 24px заголовки.

---

Хочешь — я подберу готовую **Tailwind-палитру** (варианты классов `bg-green-700`, `text-gray-800` и т.д.), чтобы вы сразу могли в коде использовать?


в системе будут юзеры сотрудники, у них будут выбор при регистрации, полиция, кызылорда тазалыгы и тд
если пользователи отмечают проблему, которая касается их делу, они видят уведомления и могут решить эту проблему(убрать мусор, дать штраф, заделать дыры на дороге и тд)

каждая решенная проблема будет отображаться в профиле самого сотрудника со временем выполнения


